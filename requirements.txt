# SentinelScreen — fully pinned runtime + development dependencies.
# Every pin is `==` so that `make reproduce` compares like-for-like binaries;
# float-level determinism in LightGBM/XGBoost is version sensitive.

# --- data plane -------------------------------------------------------------
duckdb==1.5.4
pandas==2.2.2
numpy==1.26.4
pyarrow==21.0.0

# --- sanctions screening ----------------------------------------------------
rapidfuzz==3.14.5          # Jaro-Winkler / token-set similarity (C++ core)
Metaphone==0.6             # Double Metaphone (Latin-script phonetics only)
Unidecode==1.3.8           # fallback transliteration for unmapped codepoints

# --- modelling --------------------------------------------------------------
scikit-learn==1.5.0
scipy==1.13.1
lightgbm==4.6.0
xgboost==3.2.0

# --- service layer ----------------------------------------------------------
fastapi==0.111.0
starlette==0.37.2
uvicorn==0.30.1
pydantic==2.13.3

# --- config / reporting -----------------------------------------------------
PyYAML==6.0.1
matplotlib==3.9.0

# --- test + typing ----------------------------------------------------------
pytest==9.1.0
pytest-cov==5.0.0
httpx==0.28.1              # FastAPI TestClient transport
mypy==1.10.0
types-PyYAML==6.0.12.20240808
